@model List<Models.EF.Menu>
<div class="call">
    <p><span>Hỏi đáp</span> <span class="number">Mr Đức: 0163.844.6786</span></span></p>
</div>
<div class="account_desc">
    <ul>
        @foreach (var item in Model)
        {
            <li><a href="@item.Link" target="@item.Target">@item.Text</a></li>
        }
    </ul>
    
</div>
